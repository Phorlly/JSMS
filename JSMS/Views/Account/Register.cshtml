@model JSMS.Models.RegisterViewModel
@{
    ViewBag.Title = "Register";

    Layout = null;

}
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="~/Images/logoHRMS.png">
    <title>LOGUP</title>
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    <link href="~/Theme/plugins/fontawesome-free/css/all.min.css" rel="stylesheet" />
    <style>
        h1, span {
            font-display: inherit;
            font-family: 'Khmer OS Moul','Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        p, label, a ,input{
            font-family: 'Khmer OS Battambang','Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-size: large;
        }
    </style>
</head>

<body>
    <!--New form-->
    <div style="height: 100vh; " class="d-flex justify-content-center align-items-center ">

        <section>
            <div class="modal-lg form-control d-flex  shadow-lg p-5 mb-5 bg-body-tertiary rounded-4">

                @using (Html.BeginForm("Register", "Account", FormMethod.Post, new { role = "form" }))
                {
                    @Html.AntiForgeryToken()
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <div class="bg-body-tertiary">
                        <div class="mb-3 text-center">
                            <h1 class="text-secondary text-uppercase">@JSMS.Resources.Language.CreateNewAccount</h1>
                            <p class="fst-italic">@JSMS.Resources.Language.RememberUP</p>
                        </div>
                        <div>
                            <div class="form-group mb-4">
                                <label>@JSMS.Resources.Language.Username</label>
                                <input type="text" class="form-control" name="UserName" placeholder="@JSMS.Resources.Language.Input">
                                @Html.ValidationMessageFor(m => m.UserName, "", new { @class = "text-danger" })
                            </div>

                            <div class="form-group mb-4">
                                <label>@JSMS.Resources.Language.Password</label>
                                <input type="text" class="form-control" name="Password" placeholder="@JSMS.Resources.Language.Input">
                                @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
                            </div>
                            <div class="form-group mb-4">
                                <label>@JSMS.Resources.Language.ConfirmPassword</label>
                                <input type="text" class="form-control" name="ConfirmPassword" placeholder="@JSMS.Resources.Language.Input">
                                @Html.ValidationMessageFor(m => m.ConfirmPassword, "", new { @class = "text-danger" })
                            </div>

                        </div>
                        <div class="footer">
                            <button type="submit" class="btn btn-secondary w-100 mb-2">
                                <i class="fas fa-user-plus"></i>
                                <span class="text-uppercase">@JSMS.Resources.Language.CreateAccount</span>
                            </button>
                            <div class="text-center">
                                <a href="@Url.Action("Login","Account")">@JSMS.Resources.Language.AlreadyAccount</a>
                                @*@Html.ActionLink("Already account for Login right?", "Login", "Account", new { area = "" }, new { @class = "nav-link" })*@
                            </div>
                            <div class="text-center mt-2">
                                @if (Request.Cookies["Languages"].Value == "km")
                                {
                                    <a class="nav-link" href="@Url.Action("Change","Language",new {translator = "en"})" role="button">
                                        <img src="~/Images/us.png" style=" width: 17px;"> English
                                    </a>
                                }
                                else
                                {
                                    <a class="nav-link" href="@Url.Action("Change","Language",new {translator = "km"})" role="button">
                                        <img src="~/Images/kh.png" style=" width: 17px;">
                                        <span style="font-family:Hanuman">ភាសាខ្មែរ</span>
                                    </a>
                                }
                            </div>
                        </div>

                    </div>
                }
            </div>
        </section>
    </div>

    @section Scripts {
        @Scripts.Render("~/bundles/jqueryval")
    }
</body>

</html>
